export const metadata = {
  title: 'Academics',
  description: 'Explore our curriculum, teaching methodology, and outstanding academic results.',
};

export default function Layout({ children }) {
  return <>{children}</>;
}
