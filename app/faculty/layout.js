export const metadata = {
  title: 'Our Faculty',
  description: 'Meet the dedicated teachers and principal of Matha English Medium School.',
};

export default function Layout({ children }) {
  return <>{children}</>;
}
