export const metadata = {
  title: 'Admissions',
  description: 'Admission process, age criteria, and inquiry form for Matha English Medium School.',
};

export default function Layout({ children }) {
  return <>{children}</>;
}
