export const metadata = {
  title: 'News & Events',
  description: 'Stay updated with the latest announcements, upcoming events, and holiday calendar.',
};

export default function Layout({ children }) {
  return <>{children}</>;
}
