export const metadata = {
  title: 'Gallery',
  description: 'View memories and moments from our annual days, sports events, and classroom learning.',
};

export default function Layout({ children }) {
  return <>{children}</>;
}
