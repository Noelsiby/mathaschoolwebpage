export const metadata = {
  title: 'About Us',
  description: 'Learn about the history, vision, and mission of Matha English Medium School.',
};

export default function Layout({ children }) {
  return <>{children}</>;
}
